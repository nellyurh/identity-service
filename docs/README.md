# docs/

- `ARCHITECTURE.md` — layers, mechanisms, data stores.
- `RUNBOOK.md` — health, outbox relay, CD/infra requirements.
- `API.md` — endpoint reference + error codes (full spec in `../openapi/openapi.yaml`).
- `EVENTS.md` — emitted/consumed events and payload contract.

These documents describe the service at its current milestone and grow as capabilities land.
